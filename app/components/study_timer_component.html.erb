<div class="bg-white shadow sm:rounded-lg"
     data-controller="timer"
     data-timer-duration-value="<%= duration %>"
     data-timer-goal-id-value="<%= goal.id %>">
  <div class="px-4 py-5 sm:p-6">
    <h3 class="text-base font-semibold leading-6 text-gray-900">Study Timer for <%= goal.title %></h3>
    <div class="mt-2 max-w-xl text-sm text-gray-500">
      <p>Focus on your goal with a <%= duration %> minute timer.</p>
    </div>
    <div class="mt-5">
      <div class="rounded-md bg-gray-50 px-6 py-5 sm:flex sm:items-center sm:justify-between">
        <div class="text-4xl font-mono font-bold text-gray-900" data-timer-target="display">
          25:00
        </div>
        <div class="mt-4 sm:ml-6 sm:mt-0 sm:flex sm:flex-shrink-0 sm:items-center">
          <button type="button"
                  data-timer-target="startButton"
                  data-action="timer#start"
                  class="inline-flex items-center rounded-md bg-indigo-600 px-3 py-2 text-sm font-semibold text-white shadow-sm hover:bg-indigo-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600">
            Start
          </button>
          <button type="button"
                  data-timer-target="pauseButton"
                  data-action="timer#pause"
                  disabled
                  class="ml-3 inline-flex items-center rounded-md bg-white px-3 py-2 text-sm font-semibold text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 hover:bg-gray-50">
            Pause
          </button>
          <button type="button"
                  data-timer-target="resetButton"
                  data-action="timer#reset"
                  disabled
                  class="ml-3 inline-flex items-center rounded-md bg-white px-3 py-2 text-sm font-semibold text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 hover:bg-gray-50">
            Reset
          </button>
        </div>
      </div>
    </div>
  </div>
</div>
